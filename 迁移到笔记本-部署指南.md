# 迁移到新机器（笔记本）部署指南

> 本文是给**在笔记本上操作的 AI 智能体**看的执行手册。目标：把"具身智能行业研究 RAG 问答系统"从原电脑迁移到一台**只有集成显卡（无独立 NVIDIA 卡）**的笔记本上跑起来。请逐步执行并在每步后做验证。

## 0. 背景与关键事实（执行前必读）

- 这是一个 FastAPI + LangGraph 的 RAG 问答服务，本地加载两个模型（bge-m3 嵌入、bge-reranker 精排），生成走 **DeepSeek API**、图表转写走**百炼 Qwen-VL API**（即需要联网 + 有效 API Key）。
- 笔记本是**集显**，必须用 **CPU 模式**跑（`EMBEDDING_DEVICE=cpu`）。代码会自动检测：无 NVIDIA 卡时自动回落 CPU；为保险仍在 `.env` 显式设 cpu。
- **不要重新建索引**：索引已在原机器建好（约数小时的离线流水线），通过 `data_bundle.zip` 整包搬过来即可，省去全部重算。
- **不要拷贝 `.venv/`**：里面是原机器的绝对路径 + GPU 版 torch，必须在笔记本上重建（装 CPU 版 torch）。

## 1. 需要用户预先准备好的文件（从原电脑拷来）

| 文件 | 大小 | 说明 | 必须 |
|---|---|---|---|
| `data_bundle.zip` | ~441 MB | 预建索引打包（faiss/bm25/figures/parsed 等） | ✅ |
| `.env` 的内容（含 API Key） | 几行 | **含密钥，须由用户提供**，智能体无法自行获取 | ✅ |
| `具身智能/` 文件夹（908 份 PDF） | ~4 GB | 仅"回答内插入图表/原页"功能需要；纯文字问答不需要 | ⭕ 可选 |

> 若用户没给 `data_bundle.zip`，也没给 `具身智能/` 的 PDF + 不愿等数小时重建——则**无法运行**，需向用户索要 `data_bundle.zip`。

## 2. 部署步骤

### 2.1 安装 Python 与克隆代码
确认 Python 3.10+（`python --version`）。然后：
```powershell
git clone https://github.com/shit-king/Embodied-Intelligence-RAG.git
cd Embodied-Intelligence-RAG
```

### 2.2 创建虚拟环境 + 安装依赖（CPU 版）
```powershell
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```
- **重要**：集显机器**不要**执行 README 里那条 `torch --index-url ...cu118` 命令（那是给 NVIDIA 卡的）。`pip install -r requirements.txt` 在无 N 卡机器上会自动装 **CPU 版 torch**，正确。

### 2.3 还原索引数据
把用户提供的 `data_bundle.zip` 放到项目根目录，解压**到 `data/` 文件夹**：
```powershell
Expand-Archive -Path data_bundle.zip -DestinationPath .\data -Force
```
验证结构正确（应存在以下路径）：
```powershell
Test-Path data\faiss\index.bin, data\bm25.pkl, data\figures.npy, data\indexed.json
```
四个都应返回 `True`。

### 2.4 配置 `.env`
在项目根目录创建 `.env`（密钥须由用户给出，**切勿编造**）：
```
DEEPSEEK_API_KEY=<用户的DeepSeek密钥>
VL_API_KEY=<用户的百炼密钥>
EMBEDDING_DEVICE=cpu
```

### 2.5（可选）放入 PDF 语料
若用户提供了 `具身智能/` 文件夹（4GB PDF），拷到项目根目录即可启用"回答内插图表/原页"。不提供则跳过，纯文字问答不受影响。

### 2.6 启动服务
双击 `start.bat`，或直接运行：
```powershell
.venv\Scripts\python -m uvicorn app.api.main:app --port 8000
```
- 首次启动 **CPU 加载模型约 60-90 秒**，看到 `Application startup complete` 才算就绪（不要在此之前判定失败）。

## 3. 验证（务必做）

1. 服务就绪检查（应返回 200）：
   ```powershell
   (Invoke-WebRequest http://localhost:8000/ -UseBasicParsing).StatusCode
   ```
2. 浏览器打开 http://localhost:8000，提一个问题（如"特斯拉 Optimus 的进展和量产计划"），确认：
   - 有流式回答、带 `[来源N]` 标注；
   - 下方出现来源卡片（报告名+页码）；
   - 若放了 PDF，相关问题会出现"相关图表"。

## 4. 常见问题排查

- **黑窗口秒退 / 段错误（Segmentation fault）**：几乎只发生在"有 NVIDIA 卡但 GPU 状态异常"时。集显机器不会用 GPU。确保 `.env` 里有 `EMBEDDING_DEVICE=cpu`；若机器其实有独显且报此错，重启机器或保持 cpu。
- **端口 8000 被占用**：换端口启动 `--port 8001`，浏览器开对应端口；或排查占用进程。
- **回答前等待较久（2-8 秒）**：CPU 上 rerank 较慢，属正常。嫌慢可把 `app/rag/agent.py` 里 `MAX_CANDIDATES = 30` 改为 `15`，rerank 提速约一倍、质量几乎无损。
- **图片/图表打不开（404）**：未拷 `具身智能/` PDF 所致；纯文字问答不受影响，前端会自动隐藏加载失败的图。
- **回答报错 / 无生成**：检查 `.env` 的 `DEEPSEEK_API_KEY` 是否有效、机器能否联网访问 `api.deepseek.com`。
- **首次提问比平时慢十几秒**：仅当 `data/figures.npy` 与 `data/figures.jsonl` 行数不一致时会重算图注向量；用整包 `data_bundle.zip` 还原则不会发生。
- **中文显示乱码**：`start.bat` 已是 ASCII+CRLF；若手动跑命令，先 `set PYTHONUTF8=1`。

## 5. 技术栈速览（便于智能体理解项目）

LangGraph（Agent 编排）+ DeepSeek（生成）+ bge-m3/FAISS（向量检索）+ jieba/BM25（关键词）+ bge-reranker（精排）+ Qwen-VL（图表转写）+ FastAPI/原生 JS。入口 `app/api/main.py`，配置 `app/config.py`，检索/Agent 在 `app/rag/`。各版本设计与踩坑见 `docs/v0.1`~`v0.8`。
